---
title: Look now I can load posts from filesource!
date: 2020-01-03
published: true
cover: ./cover.jpg
---

# h1 Not so much to talk about

```jsx
export default ({ data }) => {
  return (
    <>
      <Layout>
        {data.allMdx.nodes.map(({ id, excerpt, frontmatter, fields }) => (
          <Link to={fields.slug}>
            <h1>{frontmatter.title}</h1>
            <p>{frontmatter.date}</p>
          </Link>
        ))}
      </Layout>
    </>
  );
};
```